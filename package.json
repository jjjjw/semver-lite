{
  "name": "semver-lite",
  "version": "0.0.2",
  "description": "Not the semantic version parser used by npm.",
  "main": "semver.lite.js",
  "scripts": {
    "test": "tap test/*.js"
  },
  "devDependencies": {
    "tap": "0.x >=0.0.4"
  },
  "license": "MIT",
  "repository": "git://github.com/oncemoreification/semver-lite.git"
}
